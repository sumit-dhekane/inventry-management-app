import React from 'react'
import Layout from '../container/layout';
import ProductlistContainer from "../container/projectList"

export const Productlist = () => {
    return (
        <Layout>
        <ProductlistContainer/>
        </Layout>
    )
}
export default Productlist;
